import { Card, CardTitle } from '@/components/ui/Card'

const alerts = [{ title: 'New administrator added', detail: 'Google Workspace · 10 minutes ago', level: 'Review', tone: 'caution' }, { title: 'Large file export detected', detail: 'Customer register · Yesterday at 16:20', level: 'Investigate', tone: 'risk' }, { title: 'Weekly access review completed', detail: 'Workspace controls · 30 Jul 2026', level: 'Resolved', tone: 'safe' }]

export default function SecurityLogsPage() {
  return <div className="space-y-7"><section className="flex justify-between gap-4"><div><p className="text-small font-semibold uppercase tracking-[0.16em] text-safe">Security activity</p><h1 className="mt-2 font-display text-display-lg">Signals worth your attention.</h1><p className="mt-2 max-w-xl text-body text-ink-soft">A simple stream of changes that may affect your customer data.</p></div><button className="hidden h-fit rounded-control border border-ink/15 px-4 py-2 text-small font-medium text-ink sm:block">Mark all read</button></section><div className="grid gap-4 lg:grid-cols-[1fr_280px]"><Card className="p-0 overflow-hidden"><div className="border-b border-line px-5 py-4"><CardTitle>Recent alerts</CardTitle></div><div className="divide-y divide-ink/10">{alerts.map((alert) => <div key={alert.title} className="flex gap-4 px-5 py-5"><span className={`mt-0.5 h-2.5 w-2.5 shrink-0 rounded-full ${alert.tone === 'safe' ? 'bg-safe' : alert.tone === 'caution' ? 'bg-caution' : 'bg-risk'}`} /><div className="flex-1"><p className="font-medium text-ink">{alert.title}</p><p className="mt-1 text-small text-ink-soft">{alert.detail}</p></div><span className={`h-fit rounded-full px-2.5 py-1 text-caption font-medium ${alert.tone === 'safe' ? 'bg-safe/10 text-safe' : alert.tone === 'caution' ? 'bg-caution/15 text-caution' : 'bg-risk/10 text-risk'}`}>{alert.level}</span></div>)}</div></Card><Card><p className="text-caption font-semibold uppercase tracking-[0.14em] text-ink-soft">This month</p><p className="mt-4 font-data text-display-lg">03</p><p className="text-small text-ink-soft">alerts requiring review</p><div className="mt-6 border-t border-line pt-5 text-small text-ink-soft">Monitoring is active for <span className="font-semibold text-ink">2 connected tools</span>.</div></Card></div></div>
}
