import { Card, CardTitle } from '@/components/ui/Card'

const apps = [{ name: 'Google Workspace', description: 'Email, Drive and account controls', status: 'Connected', letter: 'G' }, { name: 'Slack', description: 'Team communication and files', status: 'Review access', letter: 'S' }, { name: 'Paystack', description: 'Payments and customer records', status: 'Not connected', letter: 'P' }]

export default function IntegrationsPage() {
  return <div className="space-y-7"><section><p className="text-small font-semibold uppercase tracking-[0.16em] text-safe">Connected tools</p><h1 className="mt-2 font-display text-display-lg">See your data footprint.</h1><p className="mt-2 max-w-xl text-body text-ink-soft">Map the tools that process personal data and keep vendor checks in one place.</p></section><div className="grid gap-4 md:grid-cols-3">{apps.map((app) => <Card key={app.name} className="flex min-h-56 flex-col"><div className="flex items-start justify-between"><span className="grid h-11 w-11 place-items-center rounded-xl bg-ink text-title font-semibold text-paper">{app.letter}</span><span className={`rounded-full px-2.5 py-1 text-caption font-medium ${app.status === 'Connected' ? 'bg-safe/10 text-safe' : app.status === 'Review access' ? 'bg-caution/15 text-caution' : 'bg-ink/5 text-ink-soft'}`}>{app.status}</span></div><CardTitle className="mt-6">{app.name}</CardTitle><p className="mt-2 text-small text-ink-soft">{app.description}</p><button className="mt-auto pt-6 text-left text-small font-semibold text-ink underline decoration-amber decoration-2 underline-offset-4">{app.status === 'Not connected' ? 'Connect tool' : 'Manage connection'}</button></Card>)}</div><Card className="flex flex-col justify-between gap-4 bg-ink text-paper sm:flex-row sm:items-center"><div><p className="font-display text-title">Missing a tool?</p><p className="mt-1 text-small text-paper/65">Add it manually so your data register stays complete.</p></div><button className="rounded-control bg-paper px-4 py-2.5 text-small font-semibold text-ink">Add manual record</button></Card></div>
}
